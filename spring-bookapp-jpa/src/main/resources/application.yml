#datasource specific properties
spring:
  datasource:
    username: root
    password: root
    url:  jdbc:mysql://localhost:3306/intuitdb
    #jpa specific properties
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
